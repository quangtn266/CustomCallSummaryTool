# CustomCallSummaryTool
A custom call summary tool demo load an audio, then it send a summary into any user through an email

## Configuration
```
export KMP_DUPLICATE_LIB_OK=True
```

```
pip install -r requirements.txt
```